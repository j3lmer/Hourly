{% extends 'base.html.twig' %}

{% block title %}Successfully deleted Hour entry{% endblock %}


{% block topbuttons %}
    <a class="btn btn-dark" , href="{{ path('app_project', {projectname: projectname}) }}">Back</a>
{% endblock %}


{% block body %}
    <div class="container">
    <div class="row d-flex justify-content-center rounded mt-5">
        <div class="col-md d-flex justify-content-center my-5">
            <div class="row">
                <div class="col-md">
                    <h1>Successfully deleted entry</h1>
                    <h1 class="text-secondary">{{ hour.timestampStart |format_datetime('short', 'short') }}
                        - {{ hour.timestampEnd |format_datetime('short', 'short') }}</h1>
                </div>
            </div>
        </div>
    </div>

{% endblock %}